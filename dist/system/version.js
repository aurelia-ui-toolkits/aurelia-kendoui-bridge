'use strict';

System.register([], function (_export, _context) {
  "use strict";

  var version;
  return {
    setters: [],
    execute: function () {
      _export('version', version = '1.6.0');

      _export('version', version);
    }
  };
});